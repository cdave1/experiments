#include <stdio.h>

// test a few things
// find how long it takes to stream binary data from a file using the following methods
// 1. open each file from a file system in sequence
// 2. open files from a zip file via physfs
// 3. open files from a concatted file via mmap

int fmap(const char * path)
{
  // open a file

  return 0;
}


int main (int argc, char ** argv)
{
  printf("%-32d--%-32d", 123, 456);
  return 0;
}
